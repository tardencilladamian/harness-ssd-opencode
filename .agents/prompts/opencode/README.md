# OpenCode Prompt Adapters

This folder can store project-specific prompt snippets for OpenCode.

Keep the neutral role contracts in `.agents/roles/`.

OpenCode-specific prompts must not contradict the neutral contracts.

